1. the aruco marker should be printed.

2. aruco_ros is used to track the marker. Change the input image topic if necessary
the result topic can be input to image_view to show the result

3. for robots without a ros node, pub_base_and_ee_link node is necessary to pub the ee_link

4. easy_handeye in freehand mode can be used to calibrate the camera frame and the robot frame

5. check all input topics and change them when necessary
